SCOPE OF THIS REVIEW: the diff on standard input is the WHOLE BRANCH a pull request will merge, from its merge base with the base branch to its head, not one push, and it covers EVERY file the branch changed, of every type (Swift, markdown, configuration and the rest), not only the code types listed below. Wherever the instructions below say "push", read "this branch". The file list at the top of the input names every file the branch changed. Hold every file type to the same standard, and weigh the recorded lessons above most of all: this review is the last reading before the branch merges.
